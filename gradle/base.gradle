android {
    compileSdk 35

    defaultConfig {
        minSdkVersion 24
        targetSdkVersion 35
        versionCode 36
        versionName "3.0.6"

        consumerProguardFiles "consumer-rules.pro"
    }
}