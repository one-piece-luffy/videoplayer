android {
    compileSdk 36

    defaultConfig {
        minSdkVersion 24
        targetSdkVersion 36
        versionCode 36
        versionName "3.0.6"

        consumerProguardFiles "consumer-rules.pro"
    }
}